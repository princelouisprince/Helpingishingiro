# Render Blueprint Specification to automate deployment
services:
  - type: web
    name: helpingishingiro
    env: static
    buildCommand: npm run build
    publishDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
